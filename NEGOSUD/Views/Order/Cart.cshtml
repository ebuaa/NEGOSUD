@model NEGOSUD.Models.Entities.OrderViewModel

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<h1>Panier</h1>

<form asp-action="CancelOrder" method="post" class="py-3">
	<div class="form-group">
		<input type="submit" value="Annuler" class="btn btn-danger rounded" />
	</div>
</form>


<!--Form to finalise and create the order-->
<form asp-action="PlaceOrder" method="post">
	<h3>Commande</h3>
	<table class="table table-bordered">
		<thead>
			<tr>
				<th>Produit</th>
				<th>Quantité</th>
				<th>Prix</th>
				<th>Total</th>
			</tr>
		</thead>
		<tbody>
			@for (int i = 0; i < Model.OrderDetails.Count; i++)
			{
				<tr>
					<td>@Model.OrderDetails[i].ProductName</td>
					<td>@Model.OrderDetails[i].Quantity</td>
					<td>@Model.OrderDetails[i].Price €</td>
					<td>@(Model.OrderDetails[i].Quantity * Model.OrderDetails[i].Price) €</td>
				</tr>
			}
			<tr>
				<td colspan="3" class="text-end">Total:</td>
				<td class="bg-primary bg-gradient text-white fw-bold">@Model.TotalAmount €</td>
			</tr>
		</tbody>
	</table>

	<div class="form-group">
		<input type="submit" value="Place Order" class="btn btn-primary" />
	</div>
</form>

